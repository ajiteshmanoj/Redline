# Committed, non-secret. Guarantees the public deployment runs the bulletproof
# demo (scripted fixtures, zero network, no API key). .env.local overrides this
# locally, so your live-path testing is unaffected.
DEMO_MODE=true
